spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/domeself/config-server.git
          search-paths:
server:
  port: 1201